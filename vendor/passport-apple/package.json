{
  "name": "redrockcode-vendor-passport-apple",
  "version": "0.0.0",
  "private": true,
  "description": "Isolated install of passport-apple to avoid touching the main dep tree",
  "dependencies": {
    "passport-apple": "^2.0.2"
  }
}
